"""yRDP — agent-driven remote GUIs, with lore.

A pinned viewport turns a GUI into an addressable surface, and lore turns repeat
traversal into roughly O(1).  See ``docs/architecture.md`` for the settled
design; this package is its v0 implementation.
"""

__version__ = "0.1.0"
